import styled from 'styled-components';

const StyledButton = styled.button`
  cursor: pointer;
  border-radius: 4px;
  font-size: 1rem;
  font-weight: 700;
  padding: 1rem;
`;

export { StyledButton };
